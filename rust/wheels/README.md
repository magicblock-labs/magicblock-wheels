# wheels

Public `no_std` crate for reusable Rust building blocks.

This crate is the intended entrypoint for consumers. Proc macros are implemented
in `wheels-macros` and re-exported here so users only need one Rust dependency.
